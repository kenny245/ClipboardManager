# 更新日志

本文档记录 ClipboardManager 的所有重要版本变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [Unreleased]

### 移除

- 移除剪贴板图片记录功能，仅保留文本历史
- 启动时自动迁移：清除历史中的图片条目及其缓存的 PNG 文件（`images/` 目录清空后一并删除）

### 修复

- 修复 `format_time()` 在每月 1 号计算"昨天"时 `day=0` 越界崩溃的问题（改用 `timedelta` 计算）
- 修复全局热键回调在后台消息线程中直接操作 GUI 的线程安全隐患：改为通过 Qt 信号自动转发到主线程执行
- 修复 `history.json` 直接覆写可能损坏的问题：改为「临时文件 + `os.replace`」原子写入，并强制 `fsync`

### 优化

- 历史记录写入增加 500ms 防抖，连续复制时合并为一次磁盘写入，大幅减少 I/O
- 程序退出前强制 `flush()`，确保防抖窗口内的最后几条记录不丢失
- 统一默认数据目录常量（`config.DEFAULT_DIR`），消除 `D:\剪贴板` 在三个模块中的重复硬编码

---

## [1.0.0] — 2026-07-21

### 新增

- 剪贴板历史自动记录（文本 + 图片）
- 全局快捷键 `Ctrl + Shift + V` 唤出搜索面板
- 实时关键词搜索过滤历史记录
- 系统托盘常驻，右键菜单快捷操作
- 设置面板：最大储存条数 / 储存位置 / 开机自启动 / 关闭按钮
- 搜索面板下拉/收起平滑缓动动画 (280ms)
- Windows DWM 亚克力模糊半透明窗口效果
- 搜索面板可拖拽，位置自动记忆
- 图片缩略图预览，标注尺寸信息
- 右键菜单：删除 / 恢复单条记录
- 首次发布，PyInstaller 单文件打包

[1.0.0]: https://github.com/kenny245/ClipboardManager/releases/tag/v1.0.0
